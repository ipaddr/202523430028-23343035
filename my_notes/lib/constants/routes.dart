/// Named route for the note creation / update screen.
///
/// Pass a [CloudNote] as the route argument to open an existing note;
/// omit it to create a new one.
const createOrUpdateNoteRoutes = '/notes/create-update-note';
